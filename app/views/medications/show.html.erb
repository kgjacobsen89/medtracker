<h1> <%= @medication.name %> </h1>

<h2> Doctors Familiar with <%= @medication.name %> </h2>
<ul>
	<% @medication.doctor_ids.each do |d| %>
	<li>
		<strong> Dr. <%= d.first_name %> <%= d.last_name %> </strong> <br />
		<%= d.location %> <br />
		<i><%= link_to "Doctor Profile", doctor_path(d) %></i>
	</li>
	<% end %>
</ul>




<%= link_to "Delete", :medication, method: :delete, data: { confirm: "Are you sure you want to delete #{@medication.name}?"} %>

<%= link_to "Back to the list!", user_medications_path(@user.id) %>